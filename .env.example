# Environment variables for Docker Compose

# Host directory for generated datasets (must have 100GB+ space)
DATA_MOUNT_PATH=/tmp/psi-data

# Container path for data access
DATA_PREFIX=/data

# Number of data points (default for full experiments)
NODE_SIZE=1000000000
